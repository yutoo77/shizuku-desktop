# Security

## 対象と検証範囲

現在はWindows x64・主画面1枚を対象にした技術試作です。最新版のmainを対象に不具合を調査します。独立した侵入テストや、一般向け配布物の安全性認証を受けたものではありません。

アプリはローカルVRMの表示、明示操作による窓の位置確認、定型会話と任意のOpenAI会話、任意のローカルVOICEVOX読み上げを行います。新しい会話窓は毎回ローカルの定型返答・音声オフで始まり、利用者自身のキーが設定されていても、自分でOpenAIを選んで文章を送るまで外部AIへ通信しません。音声も自分で有効にして送信した後の返答だけを対象にします。マイク、他アプリの画面取得、自律PC操作はありません。開発用の依存取得やGitHubのCIは別です。

## 脆弱性を見つけた場合

認証情報、私的なファイル、モデル本体、個人情報を公開Issueへ載せないでください。[非公開の脆弱性報告](https://github.com/yutoo77/shizuku-desktop/security/advisories/new)を利用してください。再現手順、対象commit、Windowsの版、影響を記載し、実データの代わりに最小限の再現例を使ってください。

個人開発のため応答期限は保証していません。通常の使い勝手や、機密情報を含まない不具合はIssueで扱います。

## 維持する境界

- 描画画面はsandboxとcontext isolationを有効にし、Node.jsを公開しない。
- IPCは送信元の画面・main frame・URLと引数を確認する。会話画面へモデル・PC操作の機能を渡さない。
- 画面の外部通信は拒否したまま、OpenAIへの通信をmain処理に限定する。接続先は`https://api.openai.com/v1/responses`に固定し、リダイレクト、自動再試行、ツール呼出しを行わない。
- APIキーは起動時の`OPENAI_API_KEY`をmain側で受け取る。アプリが画面・補助プロセスを作る前にその環境変数を削除し、キーを画面・IPC・設定・ログへ返さない。配布物に開発者の共有キーを含めない。
- VRMは利用者が選んだローカルファイルだけを読み、外部画像・外部データ参照を拒否する。利用条件はソースのライセンスと分ける。
- アプリ内の会話はメモリ内だけに保持し、閉じる・明示的な非表示・休止・終了時に破棄する。接続先の切替でも会話と下書きを消去し、返答待ちを中止する。入力をHTMLとして解釈しない。
- AIへ送信する履歴は完了した直近6往復・6000文字以内に制限し、中止・失敗した発言を次回の履歴に含めない。入力1000文字、返答4000文字、受信本文128KiB、待機30秒に上限を設ける。応答本文や上流エラーを実行せず、エラーは固定の案内に変換する。
- 音声はmain処理から`http://127.0.0.1:50021`の`audio_query`と`synthesis`だけへ送信する。話者は標準VOICEVOXの冥鳴ひまり・ノーマル（スタイルID 14）に固定し、画面から文章・話者・接続先を任意指定する音声IPCを公開しない。起動時の探索・自動起動・自動再試行・リダイレクトは行わない。
- 音声対象は完了した返答1000文字まで。合成用JSONは受信256KiB、音声は受信8MiB、モーラは1000件、見積もりと音声は90秒、2回の通信と受信は合計30秒に制限する。受信した項目を検証し、PCM・モノラル・24kHz・16bitのWAVだけを画面へ渡す。画面のネットワーク禁止を維持し、メディアURLを使わずメモリ上で再生する。
- 音声オフ、中止、新しい送信、消去、接続先切替、会話を閉じる、明示的な非表示、休止、終了時は準備・再生を止め、古い結果を破棄する。音声はディスクへ保存せず、口形IPCは現在の発話ID・5母音・有限の強度値に限定する。音声応答の異常や再生失敗で文字の返答を失わない。
- APIキー、モデル、録音、設定、実行ログ、生成した実行ファイルをGitに入れない。

OpenAIへは`store:false`を指定しますが、これは送信先での保存が一切ないことを意味しません。同社の不正利用監視やキャッシュ等の保存条件が適用されます。中止しても、送信済みの処理や課金が必ず止まるとは限りません。[送信内容・起動方法・公式の保存条件](docs/ai-dialogue.md)を参照してください。

VOICEVOXへの`audio_query`では返答の文章がURLのクエリに含まれます。返答に利用者の発言の引用があれば、その部分も送られます。しずくアプリは文章・音声を保存しませんが、別に起動したVOICEVOX側のログ・保存はその設定に依存し、要求URLを記録するログに返答文が残る可能性があります。OSのログも制御対象外です。固定のループバック接続先は、このPC上の信頼した標準VOICEVOXを前提にしており、同じポートを使う別プロセスの正当性を認証する仕組みはありません。通常の`synthesis`の計算は通信中止後も続く場合があります。ユーザーが起動したエンジンをアプリから終了せず、音声ソフトや音声ライブラリも同梱しません。[音声の起動手順と制約](docs/voice-dialogue.md)を参照してください。

現在は利用者自身のキーで動かす個人向けの試作です。端末や同じ利用者権限の別プロセスが侵害されてもキーを守れる設計ではありません。将来、開発者の契約で多数の利用者へ提供する場合は、認証と利用上限を備えた別のサーバーを設計し、共有キーをアプリへ配布しない方針です。

依存関係の既知の脆弱性は`npm audit`で検査します。既知の問題が0件でも、未知の問題やアプリ自身の不具合がないことの保証ではありません。[実機検証と未検証項目](docs/evaluation.md)を参照してください。
