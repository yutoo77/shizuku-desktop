# shizuku-desktop（仮称）

Windows の画面端に、背景のないキャラクターを表示する個人開発の最小試作です。月白しずくが静かにそばにいる体験を目指し、まず表示・位置変更・非表示・復帰・終了を検証します。

既存の [adaptive-vrm-dialogue-agent](https://github.com/yutoo77/adaptive-vrm-dialogue-agent) とは別アプリ・別フォルダです。会話、音声、謎解きはまだ接続していません。検証用の外見は **AvatarSample_A** で、しずく専用に制作したモデルではありません。モデル本体は同梱しません。

## 起動する

Windows と Node.js 24 以上、利用条件を確認済みのローカル VRM が必要です。PowerShell でこのフォルダを開き、順に実行します。

```powershell
npm ci
npm start
```

初回はインターネット接続が必要です。依存関係の取得に加え、Electron 44 の初回起動時に実行本体をダウンロードします。初回起動には時間がかかることがあります。以後の通常利用で外部 AI や外部サービスへの接続は行いません。

通知領域（時計の近く。隠れている場合は「^」）の月のアイコンを右クリックし、**「VRMを選ぶ…」** からファイルを選びます。初回は操作用の小窓も開きます。選べるのは画像・データをファイル内に内蔵した `.vrm` で、上限は画面表記の 100 MB（実装上 100 MiB）です。外部画像・外部データを参照するモデルは拒否します。

選択したモデルのパスと位置は `local.config.json` に保存します。この設定、モデル、`work/` の実行データは Git の管理対象外です。モデルをこのリポジトリへコピーする必要はありません。

## 動かす・隠す・終了する

キャラクターのウィンドウは**全体がクリックを背後へ通す**設定です。キャラクターを直接クリックしたり、つかんで動かしたりはできません。

| 操作 | 方法 |
| --- | --- |
| 位置を動かす | トレイの「位置を動かす…」を開き、矢印ボタンで 16 ピクセルずつ移動 |
| 隠す・再表示する | トレイの「隠す」「表示する」、アイコンのダブルクリック、または `Ctrl + Alt + Shift + S` |
| 見失った位置を戻す | トレイの「画面端に戻す」、または `Ctrl + Alt + Shift + R` |
| アプリを終了する | トレイの「終了」、または `Ctrl + Alt + Shift + Q` |

位置操作の小窓を閉じても常駐は続きます。終了すると月のアイコンが消え、起動元の PowerShell に入力待ちが戻ります。ショートカットが他のアプリと競合して登録できない場合は、トレイから操作・終了してください。

位置操作の小窓のタイトルバーを動かすとキャラクターが追従する処理もあります。ただし Windows の実際のドラッグ操作は検証が完了していません。現段階の再現手順には矢印ボタンを使ってください。表示・復帰時には主画面の作業領域内へ位置を補正します。

## 今の範囲と確かめ方

対象は Windows、主画面 1 枚、手動起動です。検証 PC には画面が 2 枚接続されていますが、主画面内だけを検証しています。複数画面への移動・異なる拡大率への対応を保証するものではありません。

描画領域は 300 × 440、描画の細かさを指定する DPR は 1、上限 30 fps です。影や床は描かず、非表示時は描画タイマーを止めます。OS の「動きを減らす」設定に対応する場合は静止表示になります。実測値・実画面の確認内容・未検証項目は [検証記録](docs/evaluation.md) にまとめます。長時間常駐、バッテリー消費、スリープ復帰、幅広い GPU や VRM での動作は今後の確認対象です。

```powershell
npm run check
npm audit
```

`check` は型検査・単体テスト・ビルドを実行します。これらの成功だけでは、Windows のクリック透過やフォーカス（キーボード入力の行き先）を確認したことにはなりません。実機では別アプリへ入力しながら表示・非表示・復帰を行い、透明部分とキャラクター部分の両方で背後をクリックできること、終了後に関連プロセスが残らないことを確認します。

## 技術の選択

Electron を試作に採用しました。既存の TypeScript・Three.js・VRM の知識を使い、Windows の透明ウィンドウと通知領域を小さな構成で扱えるためです。単なるブラウザー画面では、デスクトップ上のクリック透過や常駐の操作を同じようには検証できません。

代替の [Tauri](https://v2.tauri.app/concept/) は OS 側の WebView を使う構成で、配布容量を抑える候補です。一方、Rust 側の実装や Windows の表示・操作の再検証が増えます。今回は既存コードとの接続のしやすさを優先し、実測負荷が日常利用に適するかを先に判断します。

Electron 自体に利用料はありませんが、Chromium・Node.js を含むため実行本体の容量と複数プロセスのメモリ負荷があります。ダウンロード量、展開後容量、CPU・GPU・メモリの実測は [検証記録](docs/evaluation.md) を参照してください。配布用インストーラー、署名、更新の運用は未実装で、将来の外部 AI の料金も別です。学習・説明上の価値は、ウィンドウの振る舞い、安全なプロセス間通信、常駐負荷を測定して判断する部分にあります。

## 安全とデータ

描画側では Node.js を使えないようにし、sandbox（OS によるアクセス制限）と context isolation（画面と権限のある橋渡し処理の分離）を有効にしています。必要な操作だけを公開し、通信元と引数を検証します。画面の外部通信・外部ページへの移動・追加ウィンドウ・権限要求も制限します。基準は [Electron の公式セキュリティガイド](https://www.electronjs.org/docs/latest/tutorial/security) です。依存関係の更新管理は継続して必要です。

この試作には AI API、マイク、画面取得、自動起動、他アプリの自律操作はありません。研究コード、内部資料、参加者情報、私的な録音、市販 DLC の素材は持ち込みません。ソースコードとモデルの利用条件は別です。[第三者のライセンスとモデルの注意事項](THIRD_PARTY_NOTICES.md) を確認してください。

アプリ名は仮称で、新しい GitHub リポジトリの公開範囲は未決定です。既存リポジトリの Public 許可は引き継ぎません。新規作成時は Private を初期案とし、Public 化は別途判断します。
